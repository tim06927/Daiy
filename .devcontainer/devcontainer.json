{
  "name": "Node + Python + Playwright + Postgres DevContainer",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      },
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "postCreateCommand": "pip3 install --upgrade pip && if [ -f requirements.txt ]; then pip3 install -r requirements.txt; fi",
  "forwardPorts": [5432],
  "remoteUser": "vscode"
}
